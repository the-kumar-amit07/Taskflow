// FILE: src/hooks/useMediaQuery.js

/**
 * useMediaQuery Hook
 *
 * Detects if a CSS media query matches.
 * Used for responsive behavior that cannot be handled
 * by CSS alone (e.g., conditionally rendering components
 * on mobile vs. desktop).
 *
 * Will be implemented alongside responsive layout.
 */
