// FILE: src/utils/formatters.js

/**
 * Formatting Utility Functions
 *
 * Pure functions that transform data into display-ready formats.
 * "Pure" means they take input and return output without side effects.
 *
 * Examples:
 * - formatDate('2024-01-15') → 'Jan 15, 2024'
 * - formatRelativeTime(timestamp) → '2 hours ago'
 * - truncateText('long string...', 50) → 'long strin...'
 * - formatMemberCount(5) → '5 members'
 *
 * Will be implemented as needed.
 */