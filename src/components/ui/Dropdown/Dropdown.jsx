// FILE: src/components/ui/Dropdown/Dropdown.jsx

/**
 * Dropdown Component
 *
 * A reusable dropdown/popover menu for action menus,
 * user profile menus, and context menus.
 *
 */

export default function Dropdown({ children }) {
    return <div>{children}</div>;
}