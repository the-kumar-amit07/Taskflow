// FILE: src/components/ui/Input/Input.jsx

/**
 * Input Component
 *
 * A reusable, accessible text input with:
 * - Label support
 * - Error message display
 * - Helper text
 * - Integration with React Hook Form
 * - Different variants (outlined, filled)
 * - Icons (search icon, password toggle)
 *
 * Will be implemented in Step 16.
 */

export default function Input(props) {
  return <input {...props} />;
}
