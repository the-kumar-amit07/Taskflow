// FILE: src/app/store.js

/**
 * Redux Store Configuration
 *
 * This file creates and configures the Redux store — the single
 * source of truth for all global application state.
 *
 * Think of the Redux store like a central database that lives
 * in the browser's memory. All components can read from it,
 * and all state changes go through it.
 *
 * Will be implemented in Step 13.
 */

// Placeholder — will be implemented in Step 13
export default {};