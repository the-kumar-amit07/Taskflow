// FILE: src/app/routes.jsx

/**
 * Route Definitions
 *
 * This file defines ALL routes in the application using React Router v6.
 * Having routes in one file gives you a "map" of the entire application.
 *
 * Routes include:
 * - Public routes (login page)
 * - Protected routes (dashboard, boards — require authentication)
 * - Nested routes (board → lists → cards)
 * - Catch-all route (404 page)
 *
 * Will be implemented in Step 14.
 */

// Placeholder — will be implemented in Step 14
const routes = [];

export default routes;
